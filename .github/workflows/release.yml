name: Release

on:
  push:
    branches:
      - main
    paths:
      - Cargo.toml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  metadata:
    name: Read release version
    runs-on: ubuntu-24.04
    outputs:
      publish: ${{ steps.package.outputs.publish }}
      tag: ${{ steps.package.outputs.tag }}

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Validate package version
        id: package
        env:
          BEFORE_SHA: ${{ github.event.before }}
          EVENT_NAME: ${{ github.event_name }}
        run: |
          version="$(cargo metadata --locked --no-deps --format-version 1 | jq -r '.packages[] | select(.name == "komtar") | .version')"
          if [ -z "$version" ] || [ "$version" = "null" ]; then
            echo "Could not read the komtar package version" >&2
            exit 1
          fi

          if [ "$EVENT_NAME" = "push" ]; then
            base_ref="$BEFORE_SHA"
          else
            base_ref="$(git describe --tags --abbrev=0 --match 'v*' 2>/dev/null || true)"
          fi

          previous_version="$(
            if [ -n "$base_ref" ]; then
              git show "${base_ref}:Cargo.toml"
            fi |
              awk '
                /^\[package\]$/ { in_package = 1; next }
                /^\[/ { in_package = 0 }
                in_package && /^version[[:space:]]*=/ {
                  value = $0
                  sub(/^[^=]*=[[:space:]]*"/, "", value)
                  sub(/".*$/, "", value)
                  print value
                  exit
                }
              '
          )"

          if [ "$version" = "$previous_version" ]; then
            echo "Package version remains $version; no release needed"
            echo "publish=false" >> "$GITHUB_OUTPUT"
          else
            echo "Package version changed from ${previous_version:-unreleased} to $version"
            echo "publish=true" >> "$GITHUB_OUTPUT"
          fi
          echo "tag=v$version" >> "$GITHUB_OUTPUT"

      - name: Require a new version
        if: steps.package.outputs.publish == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          RELEASE_TAG: ${{ steps.package.outputs.tag }}
        run: |
          if gh api "repos/$GH_REPO/git/ref/tags/$RELEASE_TAG" >/dev/null 2>&1; then
            echo "$RELEASE_TAG already exists; bump the Cargo.toml version" >&2
            exit 1
          fi

  test:
    name: Test
    if: needs.metadata.outputs.publish == 'true'
    needs: metadata
    runs-on: ubuntu-24.04

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Check formatting
        run: cargo fmt --check

      - name: Run Clippy
        run: cargo clippy --locked --all-targets --all-features -- -D warnings

      - name: Run Rust tests
        run: cargo test --locked

      - name: Check crates.io package
        run: cargo publish --locked --dry-run

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          cache: npm

      - name: Install JavaScript lint dependencies
        run: npm ci

      - name: Lint JavaScript
        run: npm run lint

      - name: Set up uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          enable-cache: true

      - name: Install browser test dependencies
        run: |
          uv sync --locked
          uv run playwright install --with-deps chromium

      - name: Run browser tests
        run: uv run --locked pytest

  build:
    name: Build ${{ matrix.name }}
    if: needs.metadata.outputs.publish == 'true'
    needs:
      - metadata
      - test
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS ARM64
            runner: macos-14
            target: aarch64-apple-darwin
            force-cross: false
          - name: Linux AMD64
            runner: ubuntu-24.04
            target: x86_64-unknown-linux-musl
            force-cross: true
          - name: Linux ARM64
            runner: ubuntu-24.04
            target: aarch64-unknown-linux-musl
            force-cross: true

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Build release binary
        uses: houseabsolute/actions-rust-cross@21b0f18dc621b25bfae556ff2791fca4173121e8 # v1.0.8
        with:
          command: build
          target: ${{ matrix.target }}
          args: "--locked --release"
          cross-version: v0.2.5
          force-use-cross: ${{ matrix.force-cross }}
          use-rust-cache: false

      - name: Verify static Linux binary
        if: runner.os == 'Linux'
        env:
          BINARY: target/${{ matrix.target }}/release/komtar
        run: |
          file "$BINARY"
          file "$BINARY" | grep -Eq 'statically linked|static-pie linked'

      - name: Verify macOS ARM64 binary
        if: runner.os == 'macOS'
        env:
          BINARY: target/${{ matrix.target }}/release/komtar
        run: |
          file "$BINARY"
          file "$BINARY" | grep -q 'arm64'

      - name: Package binary
        env:
          TARGET: ${{ matrix.target }}
        run: |
          mkdir -p dist
          tar -C "target/$TARGET/release" -czf "dist/komtar-$TARGET.tar.gz" komtar
          shasum -a 256 "dist/komtar-$TARGET.tar.gz" > "dist/komtar-$TARGET.tar.gz.sha256"

      - name: Upload workflow artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: komtar-${{ matrix.target }}
          path: dist/*
          if-no-files-found: error

  publish:
    name: Publish to crates.io
    if: needs.metadata.outputs.publish == 'true'
    needs:
      - metadata
      - build
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      id-token: write
    env:
      CRATES_IO_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # A token is required for the first release. Once trusted publishing is
      # configured on crates.io, remove the secret to use short-lived OIDC tokens.
      - name: Authenticate with crates.io
        if: env.CRATES_IO_TOKEN == ''
        id: auth
        uses: rust-lang/crates-io-auth-action@c6f97d42243bad5fab37ca0427f495c86d5b1a18 # v1.0.5

      - name: Publish crate
        env:
          CARGO_REGISTRY_TOKEN: ${{ env.CRATES_IO_TOKEN || steps.auth.outputs.token }}
        run: cargo publish --locked

  release:
    name: Publish GitHub release
    if: needs.metadata.outputs.publish == 'true'
    needs:
      - metadata
      - build
      - publish
    runs-on: ubuntu-24.04
    permissions:
      contents: write

    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: dist
          merge-multiple: true

      - name: Create release and upload artifacts
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          RELEASE_SHA: ${{ github.sha }}
          RELEASE_TAG: ${{ needs.metadata.outputs.tag }}
        run: |
          if ! gh api "repos/$GH_REPO/git/ref/tags/$RELEASE_TAG" >/dev/null 2>&1; then
            tagged_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
            tag_object="$(gh api --method POST "repos/$GH_REPO/git/tags" \
              -f tag="$RELEASE_TAG" \
              -f message="Release $RELEASE_TAG" \
              -f object="$RELEASE_SHA" \
              -f type=commit \
              -f 'tagger[name]=github-actions[bot]' \
              -f 'tagger[email]=41898282+github-actions[bot]@users.noreply.github.com' \
              -f "tagger[date]=$tagged_at" \
              --jq .sha)"
            gh api --method POST "repos/$GH_REPO/git/refs" \
              -f ref="refs/tags/$RELEASE_TAG" \
              -f sha="$tag_object" >/dev/null
          fi

          if ! gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            gh release create "$RELEASE_TAG" dist/* \
              --verify-tag \
              --title "$RELEASE_TAG" \
              --generate-notes
          else
            gh release upload "$RELEASE_TAG" dist/* --clobber
          fi
